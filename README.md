Si es por primera vez, es necesario configurar la base de datos:

1. Configurar el .env

2. Ejecutar migraciones:
    ```
    npm install

    npx drizzle-kit push
    ```
        

Run:
```
npm run dev
```